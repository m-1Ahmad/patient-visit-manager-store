import { LetteronlyDirective } from './letteronly.directive';

describe('LetteronlyDirective', () => {
  it('should create an instance', () => {
    const directive = new LetteronlyDirective();
    expect(directive).toBeTruthy();
  });
});
